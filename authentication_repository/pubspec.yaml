name: authentication_repository
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.2
  firebase_auth: ^3.2.0
  firebase_core: ^1.10.0
  firebase_storage: ^10.1.0
  cloud_firestore: ^3.1.0
  google_sign_in: ^5.0.4
  meta: ^1.3.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.example.authentication_repository
    iosBundleIdentifier: com.example.authenticationRepository
